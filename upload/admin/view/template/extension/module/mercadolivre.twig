{{ header }} {{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">

            <div class="pull-right">
                <button type="submit" form="form-module" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-check-circle"></i></button>
                <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a>
            </div>

            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li><a href="{{ breadcrumb.href }} ">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>

    <div class="container-fluid">
        {% if error_warning %}
            <div class="alert alert-danger"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
                <button type="button" class="close" data-dismiss="alert">&times</button>
            </div>
        {% endif %}

        {% if success %}
            <div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> {{ success }}
                <button type="button" class="close" data-dismiss="alert">&times;</button>
            </div>
        {% endif %}

        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><i class="fa fa-pencil"></i> {{ heading_title }}</h3>
            </div>

            <div class="panel-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" class="form-horizontal" id="form-mercadolivre-config">
                    <ul class="nav nav-tabs">
                        <li class="active"><a href="#tab-general" data-toggle="tab">{{ tab_general }}</a></li>
                        <li><a href="#tab-product-settings" data-toggle="tab">{{ tab_data }}</a></li>
                    </ul>

                    <div class="tab-content">
                        <div class="tab-pane active" id="tab-general">

                            <div class="form-group required">
                                <label class="col-sm-2 control-label" for="input-app-id"><span data-toggle="tooltip">{{ entry_app_id }}</span></label>
                                <div class="col-sm-10">
                                    <input type="number" name="module_mercadolivre_app_id" value="{{ module_mercadolivre_app_id }}" placeholder="{{ entry_app_id }}" class="form-control" id="input-app-id" maxlength="30" required/>
                                    {% if error_app_id %}
                                        <div class="text-danger">{{ error_app_id }}</div>
                                    {% endif %}
                                </div>
                            </div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label" for="input-app-secret"><span data-toggle="tooltip" title="{{ help_app_secret }}">{{ entry_app_secret }}</span></label>
                                <div class="col-sm-10">
                                    <input type="text" name="module_mercadolivre_app_secret" value="{{ module_mercadolivre_app_secret }}" placeholder="{{ entry_app_secret }}" class="form-control" id="input-app-secret" maxlength="40" required/>
                                    {% if error_app_secret %}
                                        <div class="text-danger">{{ error_app_secret }}</div>
                                    {% endif %}
                                </div>
                            </div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label" for="input-country">{{ entry_country }}</label>
                                <div class="col-sm-10">
                                    <select name="module_mercadolivre_country" class="form-control" id="input-country" onchange="loadListingsTypes(this.value)">
                                        <option value="">{{ entry_select }}</option>

                                        {% for country in countries %}
                                            <option value="{{ country.key }}" {% if country.key == module_mercadolivre_country %} selected {% endif %}>{{ country.value }}</option>
                                        {% endfor %}
                                    </select>
                                    {% if error_app_country %}
                                        <div class="text-danger">{{ error_app_country }}</div>
                                    {% endif %}
                                </div>
                            </div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label" for="input-status">{{ entry_status }}</label>
                                <div class="col-sm-10">
                                    <select name="module_laquila_status" id="input-status" class="form-control">
                                        <option value="0">{{ text_disabled }}</option>
                                        <option value="1" {% if module_laquila_status %} selected="selected" {% endif %}>{{ text_enabled }}</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="tab-pane" id="tab-product-settings">
                            <div class="alert alert-info"><i class="fa fa-info-circle"></i>Configurações a ser aplicadas em produtos que não tenham nenhuma configuração selecionada personalizada no produto.</div>

                            <div class="form-group required">
                                <label class="col-sm-2 control-label" for="input-listing-type">{{ entry_ad_type }}</label>
                                <div class="col-sm-10">
                                    <select name="module_mercadolivre_listing_type" class="form-control" id="input-listing-type">
                                        <option value="">{{ entry_select_country }}</option>
                                    </select>
                                    {% if error_listing_type %}
                                        <div class="text-danger">{{ error_listing_type }}</div>
                                    {% endif %}
                                </div>
                            </div>

                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="modal" tabindex="-1" role="dialog" id="modalImportImages">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Cadastrar imagens por planilha laquila</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form action="{{ import_products_images }}" method="post" id="import_product_image" enctype="multipart/form-data">
                        <div class="form-group">
                            <label class="control-label" for="input-file-import">{{ entry_file_import }}</label>
                            <input type="file" name="document" placeholder="{{ entry_file_import }}" id="input-file-import" class="form-control" required accept="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/vnd.ms-excel, application/excel"/>
                        </div>

                        <div class="form-group text-center">
                            <button type="button" onclick="$('#import_product_image').submit();" class="btn btn-success">{{ button_send }} <i class="fa fa-send"></i></button>
                        </div>
                        <p>Envie a planilha de cadastro laquila para obter as imagens por ela</p>

                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Fechar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script type="text/javascript">

        function loadListingsTypes(country) {
            if (country) {
                $.ajax({
                    url: `https://api.mercadolibre.com/sites/${country}/listing_types`,
                    dataType: 'json',
                    method: 'GET',
                    success: function (json) {
                      if (json) {
                          var $select = $('#input-listing-type');
                          $select.html('<option value="">{{ entry_select }}</option>');

                          $.each(json, function (i, v) {
                              $select.append(`<option value="${v.id}">${v.name}</option>`);
                          });
                      }
                    }
                });
            }
        }
    </script>

{{ footer }}